POSTGRES_USER=wine_cellar
POSTGRES_PASSWORD=wine_cellar
POSTGRES_DB=wine_cellar

# Secret for the SearXNG container (any random string: openssl rand -hex 32)
SEARXNG_SECRET=
# Where the API reaches Ollama; the default is Ollama on the Docker host.
#OLLAMA_URL=http://host.docker.internal:11434
